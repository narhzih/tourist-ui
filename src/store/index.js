import { createStore } from 'vuex';

export default createStore({
  state: {
    server: {
      requestUri: 'http://localhost:300/api/v1',
    },
  },
  mutations: {},
  actions: {},
  modules: {},
});
